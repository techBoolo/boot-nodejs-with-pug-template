# Nodejs, Pug template engine 

To boot up from for an app which uses pug template engine, instead of creating
everytime we want to create an app with template engine, clone this app and
build on top of it.
